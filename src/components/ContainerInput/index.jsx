import React from "react";
import StyledContainerInput from "./StyledContainerInput";

const ContainerInput = ({ ...rest }) => (
  <div>
    <StyledContainerInput {...rest} />
  </div>
);

export default ContainerInput;
